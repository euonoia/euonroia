# ---- Frontend Dockerfile (Development) ----
FROM node:22-alpine

# Set working directory
WORKDIR /app

# Copy package files and install dependencies
COPY package*.json ./
RUN npm install

# Copy source code
COPY . .

# Expose Vite dev server port
EXPOSE 5173

# Enable hot reload (Vite)
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
